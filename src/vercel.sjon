{
  "buildCommand": "node setenv.js && npm run build",
  "outputDirectory": "dist/bouquet-shop/browser",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}